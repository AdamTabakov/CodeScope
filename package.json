{
  "name": "code-scope-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
